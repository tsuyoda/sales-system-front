import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  tableContainer: {
    borderRadius: 10,
    marginTop: 30
  },
  table: {
    minWidth: 650
  }
});

export default useStyles;
